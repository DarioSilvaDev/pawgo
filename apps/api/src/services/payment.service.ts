// Placeholder para futura integración con MercadoPago
// Este servicio se implementará cuando se active el flujo de pagos real

// import { PrismaClient } from '@prisma/client';
// import { CreatePaymentDto, Payment } from '@pawgo/shared';
// import { PaymentStatus } from '@pawgo/shared';
//
// const prisma = new PrismaClient();
//
// export const paymentService = {
//   async createPreference(data: CreatePaymentDto): Promise<string> {
//     // Integrar con MercadoPago SDK
//     // Crear preferencia de pago
//     // Guardar payment en DB con status 'pending'
//     // Retornar preference ID
//   },
//
//   async handleWebhook(webhookData: any): Promise<void> {
//     // Procesar webhook de MercadoPago
//     // Actualizar estado del pago
//     // Actualizar estado de la orden si corresponde
//   },
//
//   async getById(id: string): Promise<Payment | null> {
//     // Obtener pago por ID
//   },
// };

